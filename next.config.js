/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // domains: ['logowik.com'],
  },
  experimental: {
    // Remove or correct the appDir option
    // appDir: true,
  },
}

module.exports = nextConfig